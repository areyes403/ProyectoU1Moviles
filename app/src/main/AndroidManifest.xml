<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="mx.edu.itm.link.dadm_a_u1appescolar">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DADM_A_U1AppEscolar">
        <activity android:name=".ExamenActivity"></activity>
        <activity android:name=".MostrarMateriasActivity" />
        <activity android:name=".MostrarMaterias" />
        <activity android:name=".MainActivity2" />
        <activity android:name=".ScheduleActivity" />
        <activity android:name=".AcademicAdvanceActivity" />
        <activity android:name=".KardexActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".MenuActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>